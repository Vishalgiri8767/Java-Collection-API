Map=>
    Map is an interface which is present in java.util package.
    Map does not inherit collection interface.

    Properties of map:
    -Map stores the data in key value pair.
    -In map keys should be unique but values can be same.
    -In map we can store maximun one null value in key but in values
        we can store any number of null values.
    -Map does not follow the sorting and insertion order.